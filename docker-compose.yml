version: '3.9'

services:
  chirm:
    build: .
    image: chirm:latest
    container_name: chirm
    restart: unless-stopped
    ports:
      - "8080:8080"           # Change left port to expose differently, e.g. "80:8080"
    volumes:
      - chirm-data:/data      # Persistent SQLite DB + uploaded files
    environment:
      PORT: "8080"
      DATA_DIR: "/data"
      JWT_SECRET: "${JWT_SECRET:-change-me-use-a-long-random-string}"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/api/setup/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  chirm-data:
    driver: local

# ── Traefik example (optional, for reverse proxy with SSL) ────────────────────
# Uncomment and configure if using Traefik:
#
#   labels:
#     - "traefik.enable=true"
#     - "traefik.http.routers.chirm.rule=Host(`chat.yourdomain.com`)"
#     - "traefik.http.routers.chirm.entrypoints=websecure"
#     - "traefik.http.routers.chirm.tls.certresolver=letsencrypt"
